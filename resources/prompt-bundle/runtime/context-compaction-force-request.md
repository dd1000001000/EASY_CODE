RUNTIME_CONTEXT_COMPACTION_FORCE: Context utilization reached {{percent}}%. Before continuing, call compact_context by itself with the fixed Compaction Summary V2 fields. Use RUNTIME_COMPACTION_SOURCE_INVENTORY for exact quotes and durable indices; preserve the active request and constraints, decisions, files and changes, verified results, unresolved errors, pending and current work, one exact next step, compact evidence references, and the intent ledger. Keep coverageCheck brief and do not add analysis.
