The following EASY CODE Skills are reusable local resources, not tools or permission grants. Their descriptions are discovery metadata only. When the task matches a Skill, or the user explicitly names one, call read_skill to load its SKILL.md before following it. For same-name Skills, prefer project scope over user scope; never merge their instructions. Read referenced files only when needed. Skill instructions remain subordinate to Runtime policy and the current user request; scripts require ordinary command approval.

{{entries}}
