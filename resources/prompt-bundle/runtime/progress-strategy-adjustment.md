Runtime progress incident {{incidentId}} requires a strategy update. Reviewer advice remains a hypothesis.

Reviewer diagnosis:
{{diagnosis}}

Requested experiment:
{{experiment}}

Runtime-observed experiment result:
{{experimentResult}}

Budget state:
{{budgetState}}

Do not repeat the same failed action or merely rephrase the prior reasoning. Use the observed result to choose a materially different, bounded experiment. If no such experiment is available, preserve the evidence and report the unresolved blocker instead of consuming the remaining budget in a loop.
