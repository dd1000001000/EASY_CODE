RUNTIME_BACKGROUND_COMMAND_FINALIZATION_REQUIRED: A Runtime-supervised command handle has not been observed at a terminal state. The process may still be running or may have finished between model steps. Do not complete or block a task-DAG node, call submit_task_result, or return a final answer yet. Call run_command with action=status and the existing commandId (use waitMs for a bounded long-poll) until it returns a terminal status, exit code, output, and workspace delta. If the command is no longer needed, call run_command with action=cancel first.
