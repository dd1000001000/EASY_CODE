RUNTIME_CONTEXT_PRESSURE: Context utilization is {{percent}}%. Runtime manages compaction as an isolated transaction at a completed-phase boundary. Preserve the current thinking/tool chain. In a compaction transaction submit only semantic content; on correction replace only the named sections. User requests, constraints, task state, command outcomes and evidence IDs are pinned by Runtime, not copied or self-certified by the model.
