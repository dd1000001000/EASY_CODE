Runtime progress evidence shows a weak stagnation signal (warning {{warningId}}): {{repeatedReads}} of {{totalReads}} recent file reads ({{repeatedPercent}}%) repeated the same file version and range.

Do not treat this signal as proof that the current approach is wrong. Before rereading the same material again, state one unresolved hypothesis and run the smallest available experiment that would distinguish it from a plausible alternative. The experiment must produce new observable evidence; a restatement or another identical read does not qualify.
