RUNTIME_CONTEXT_PRESSURE: Short-term context is approximately {{percent}}% of its effective active working-set capacity. After the next meaningful milestone, consider calling compact_context by itself with the fixed Compaction Summary V2 fields. Use RUNTIME_COMPACTION_SOURCE_INVENTORY for exact quotes and durable indices; do not add analysis. This is advisory, so other work may continue.
