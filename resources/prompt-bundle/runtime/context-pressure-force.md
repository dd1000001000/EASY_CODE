RUNTIME_CONTEXT_COMPACTION_FORCED: Short-term context is approximately {{percent}}% of its effective active working-set capacity. Runtime has inserted an explicit compaction request. Call compact_context by itself now with the fixed Compaction Summary V2 fields. Use RUNTIME_COMPACTION_SOURCE_INVENTORY for exact quotes and durable indices, keep coverageCheck brief, and do not add analysis; every other action is rejected until an integrity-valid compaction succeeds.
