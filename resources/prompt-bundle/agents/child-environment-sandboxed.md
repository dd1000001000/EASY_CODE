- Your task checkout is {{environmentKind}}. Worktree isolation prevents code collisions. Normal CLI commands use the platform-native OS sandbox and host toolchain while writes remain limited to this checkout and Runtime temporary roots. File tools use workspace-relative paths. Benchmark commands stay in their offline worker. Full access is a separate unsandboxed mode.
