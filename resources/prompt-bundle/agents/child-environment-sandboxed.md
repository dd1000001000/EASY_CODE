- Your physical execution environment is {{environmentKind}}. Worktree isolation prevents code collisions. Normal CLI commands run in a private Linux Podman container with this checkout mounted at /workspace; file tools still use workspace-relative paths. Benchmark commands stay in their offline worker. Native host execution requires separate explicit approval.
