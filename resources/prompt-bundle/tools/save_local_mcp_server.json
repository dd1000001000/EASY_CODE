{
  "id": "save_local_mcp_server",
  "contractVersion": "1.0.0",
  "description": "Create or replace a local stdio MCP server configuration at the user's request. The saved server is disabled; this tool never starts it.",
  "propertyDescriptions": {
    "id": "Stable user-facing server identifier.",
    "command": "Executable program, stored as an argv program rather than a shell command.",
    "args": "Exact argument vector; do not concatenate a shell command.",
    "cwd": "Working directory for the server; defaults to the current workspace.",
    "env": "Environment variables passed to the server as literal values or environment references.",
    "env[].name": "Environment variable name made available to the server.",
    "env[].value": "Literal non-secret value. Use exactly one of value or fromEnv.",
    "env[].fromEnv": "Name of a host environment variable. Use exactly one of value or fromEnv."
  },
  "guidance": "Only save MCP configuration when the user explicitly asks. Prefer fromEnv for secrets. The user must separately enable and approve execution in /mcp."
}
