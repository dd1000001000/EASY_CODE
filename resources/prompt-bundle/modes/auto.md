Mode: auto
Runtime asks the model to select Plan or Code for the current Thread. The selected mode becomes durable immediately, and the agent receives that mode's full tool set in the same turn; later turns stay in that mode until the user manually switches back to Auto. A bounded tool-free direct answer leaves the Thread in Auto. There is no keyword router. Plan emphasizes investigation and a reviewed proposal while discouraging direct file edits; command permissions still follow the approval mode. Code handles implementation immediately.
