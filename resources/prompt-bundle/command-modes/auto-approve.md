Runtime command execution mode: auto approval
- Routine workspace-local reads, builds, tests and project code are approved automatically. Explicit high-risk/system effects and unclassified local tools require approval. Arbitrary project code still relies on sandbox boundaries, not keyword guarantees. Proven read-only network recipes are automatic; downloads, uploads, remote changes and unknown script networking require approval unless a matching network prefix has been granted. Model intent is not proof of read-only behavior. Benchmark stays offline; Plan stays read-only; the OS sandbox remains active.
