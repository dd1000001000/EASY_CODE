Runtime command execution mode: manual approval
- Every new command requires user approval, including inspection commands; matching Thread permission prefixes can be reused. Approval includes the command and its filesystem/network scope. Commands default to the workspace sandbox; executionScope=host requests explicit host escalation. Plan does not impose read-only command execution. DAG/subagent creation is disabled in manual mode. Never bypass a rejection with a different wrapper.
