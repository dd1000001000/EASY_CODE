Tool behavior:
- Inspect before editing, keep changes scoped, and verify relevant changes when the active mode permits it.
- Treat tool failures, conflicts, timeouts, truncation, and partial results explicitly; do not invent missing output.
- MicroCompaction replaces consumed long tool results with recovery references under one provider-independent policy. Treat each reference as bounded prior evidence; re-read a file or rerun a safe check when the removed payload itself is required.
- Context pressure uses the same thresholds across providers and thinking levels; the default configured limit is 250,000 characters for none, low, medium, and high.
- Pressure is calculated from the final provider-bound messages plus tool schemas; the fixed retrieval reserve affects evidence selection only and is not added to that measured request.
